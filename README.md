# Installation

`npm install`

# Spin Up the Dev Server

`npm start`

# Build the Website

`npm run build`

# Deploy the Website

Make sure you have Gulp installed

`npm install -g gulp-cli`

`gulp deploy` 